console.log("This is the main file of the project");

const question = "Why are teenagers so odd?";
const answer = "Because they can't even!";

const foo = "test2";

const noDaysOff = "code code code";
